import * as React from "react";

//Layout
import Layout from "../../components/layout";

export interface OpenDataProps {}

const OpenData: React.SFC<OpenDataProps> = () => {
  return (
    <Layout>
      <h1>open data</h1>
    </Layout>
  );
};

export default OpenData;
